meta {
  name: List Events
  type: http
  seq: 1
}

get {
  url: {{base_url}}{{api_prefix}}/events
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200 && res.body.data?.length > 0) {
    bru.setEnvVar("event_id", res.body.data[0].id);
  }
}
